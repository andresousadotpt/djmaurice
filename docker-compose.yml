services:
  bot:
    build: .
    env_file: .env
    depends_on:
      pot-provider:
        condition: service_started
    restart: unless-stopped

  pot-provider:
    image: brainicism/bgutil-ytdlp-pot-provider
    environment:
      TOKEN_TTL: 6
    restart: unless-stopped
